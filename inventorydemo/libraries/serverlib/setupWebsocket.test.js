const setupWebsocket = require('./setupWebsocket');
// const eventHelperFuncs = require('./events/eventHelperFuncs'); //? -> this seems not to be necessary for test to run

test('testing setupWebsocket', () => {
  console.log('setupWebsocket test ran');
})